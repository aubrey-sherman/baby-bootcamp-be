

// test that authentication works

// test that Unauthorized error is raised if no matching user

// test that Unauthorized error is raised for wrong password

// test that registration works

// test for Bad Request error with duplicate data

// that that you can get user (needed for successful login)

// Not Found if no such user